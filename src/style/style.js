import { StyleSheet } from 'react-native-web'
import * as React from 'react'

export default StyleSheet.create({
    baseText: {
        color: '#43484d',
        fontSize: 15,
        color: 'white',
        textAlign: 'center',
        fontSize: 28,
    },
    listTitle: {
        color: 'green',
        fontSize: 20,
    },
})
